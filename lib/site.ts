/**
 * Bump LOGO_VERSION when you replace public/logo.png so browsers and PWA
 * service worker fetch the new image instead of serving a cached copy.
 */
export const LOGO_VERSION = 4
export const LOGO_SRC = `/logo.png?v=${LOGO_VERSION}`
